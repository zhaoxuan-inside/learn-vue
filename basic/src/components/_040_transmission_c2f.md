# defineProps

- **数据接收点**：父 → 子，接收外部传入的数据
- **只读**：子组件不可直接修改，保证单向数据流
- **类型安全**：支持 TypeScript 泛型声明
- **编译时宏**：仅限 `<script setup>`，无需导入

# defineEmits

- **数据发送点**：子 → 父，触发事件传递数据
- **通知机制**：子组件通过 `emit` 通知父组件执行修改
- **类型约束**：可声明事件名称及参数类型
- **编译时宏**：仅限 `<script setup>`，无需导入
