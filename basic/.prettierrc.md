配置 prettier 格式化

```json
{
  // 使用单引号，而非双引号
  "singleQuote": true,
  // 语句末尾不加分号
  "semi": false,
  // 使用2个空格进行缩进
  "tabWidth": 2,
  // 禁止使用Tab进行缩进
  "useTabs": false,
  // 对象或数组最后一个元素后不加逗号
  "trailingComma": "none",
  // 每行代码的最大宽度，超出会换行
  "printWidth": 100
}
```
